scalar Upload
scalar UUID

type Query{
  sayHello(name: String!): String!
  findAllError: [Error]!
}

type Mutation {
  upload(file: Upload): ID!
  dropCollections: Boolean!
}

type Error{
    id: ID!
    fileName: String
    timestamp: String
    failedMessage: String
    cause: String
}